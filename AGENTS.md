# SelfProduction — AI 에이전트용 작업 지침

이 저장소는 **게임을 하나 만드는 프로젝트가 아니라, 게임을 만들어내는 도구(GameForge)** 를 담고 있다.
Claude Code / Cursor / 그 밖의 어떤 AI 도구로 열든 아래 규칙은 동일하다.

---

## 0. 먼저 읽을 것

| 문서 | 내용 |
|---|---|
| `docs/01-개발-진행-방식.md` | 기획서를 받고 나서 무엇이 어떤 순서로 일어나는가 |
| `docs/02-툴-가이드.md` | GameForge 의 구조와 명령어 전체 |
| `docs/03-리소스-넣는-법.md` | 이미지·사운드를 어디에 어떤 규칙으로 넣는가 |
| `Tools/GameForge/README.md` | 툴 자체의 짧은 요약 |

---

## 1. 절대 규칙

**생성된 파일을 직접 수정하지 않는다.**
`Assets/Games/**` 와 `Sim/**` 는 전부 자동 생성물이다. 파일 첫 줄에 `<auto-generated>` 가 붙어 있다.
고쳐야 할 것이 있으면 **기획서(`Specs/*.json`) 또는 생성기(`Tools/GameForge/Gen/*.cs`)** 를 고치고 다시 생성한다.
생성물을 직접 고치면 다음 `gen` 에서 통째로 덮어써진다.

**커밋하지 않는다.**
`git commit` / `git add` 를 실행하지 말 것. 변경 요약만 보고하고 멈춘다.
형상관리는 사용자가 Fork(fork.dev) GUI 로 직접 한다.

**유니티는 C# 9 까지만 지원한다.**
파일 범위 네임스페이스(`namespace X;`), 목록 패턴 등 C# 10+ 문법을 생성 코드에 쓰면 `CS8773` 로 깨진다.
검증용 프로젝트 `Tools/UnityCheck` 가 `LangVersion 9.0` 으로 맞춰져 있으니 반드시 그것으로 확인한다.

---

## 2. 환경

| 항목 | 값 |
|---|---|
| Unity | `6000.3.11f1` (`C:/Program Files/Unity/Hub/Editor/6000.3.11f1`) |
| .NET SDK | 9.x — `dotnet` 명령으로 툴/시뮬 전부 빌드 |
| 프로젝트 루트 | `C:/UNITY_LG/SelfProduction` |
| 빌드 타겟 | **모바일 전용, 안드로이드가 메인** (iOS 는 차순위). PC/콘솔 고려 안 함 |
| 화면 방향 | 세로(Portrait) 기준 — referenceResolution 1080x1920 |

**UI 는 화면비 대응이 기본이다 (2026-08-21 확정).** 생성되는 모든 게임의 HUD 는:
- `AdaptiveScaler` — 화면비에 따라 CanvasScaler 기준 축 자동 전환(길쭉한 폰=가로 기준, 태블릿으로 갈수록 세로 기준)
- `SafeAreaFitter` — 노치/펀치홀 회피. 전체 배경(BG)만 캔버스 직속, 나머지 UI 는 전부 `UiKit.SafeArea` 루트 아래
- 이 규칙은 `Tools/GameForge/Gen/UnityEmitter.cs` 의 UiKit/Hud 템플릿에 구워져 있다. 새 이미터를 추가할 때도 따를 것.
- 유니티 프로젝트 설정(세로 고정·IL2CPP·ARM64·minSdk24·Android 타겟 전환)은
  **메뉴 > GameForge > <게임> > 모바일 설정 적용 (안드로이드)** 한 번으로 잡는다. 새 유니티 프로젝트를 받으면 이것부터.

**유니티를 켜지 않고 검증하는 것이 기본이다.** 사용자가 에디터를 열어둔 채 작업할 때가 많아
`-batchmode` 는 "another Unity instance is running" 으로 막힌다. 아래 두 경로를 쓴다.

```bash
# 1) 게임 로직(Core) + 시뮬레이터 — 순수 C#
dotnet build Sim/<GameName>/<GameName>.Sim.csproj

# 2) 유니티 레이어 — 유니티 관리 DLL 을 직접 참조해 컴파일만 확인
dotnet build Tools/UnityCheck/UnityCheck.csproj

# 3) 입력 분기까지 확인하려면 두 설정 모두 돌려볼 것
dotnet build Tools/UnityCheck/UnityCheck.csproj -p:DefineConstants="ENABLE_LEGACY_INPUT_MANAGER"
```

---

## 3. 작업 루프

```
기획서(ppt/txt)  →  Specs/<name>.json  →  validate  →  gen  →  build  →  --tune  →  apply  →  gen
                     (AI 가 옮김)         (툴)        (툴)    (검증)    (시뮬)     (툴)     (재생성)
```

문서를 스펙으로 옮기는 단계만 사람/AI 의 판단이 들어가고, 그 뒤는 전부 명령어다.
`gameforge schema` 로 쓸 수 있는 필드를 확인한 뒤 옮긴다.

---

## 4. 자주 하는 실수

- **장르 모듈을 추가하고 `EditorEmitter.IconLoop` 에 등록을 빼먹음** → 에디터 스크립트가 없는 필드를 참조해 컴파일 실패
- **`SimEmitter.ProgramFor` 에 등록을 빼먹음** → 새 장르가 방치형 시뮬을 받아서 엉뚱하게 돈다
- **`Core` 에 `UnityEngine` 을 참조** → dotnet 검증 경로가 통째로 깨진다. Core 는 순수 C# 을 유지할 것
- **밸런스가 이상할 때 수치를 손으로 만짐** → `--tune` 이 그 일을 한다. 튜너에 축이 없으면 축을 추가하는 쪽이 맞다
- **`view: "ui"` 인데 `WorldView` 를 참조** → UI 모드에서는 그 파일이 생성되지 않는다. 생성 시점에 갈라야 한다
- **레거시 `Input` 을 무조건 사용** → 이 프로젝트는 Active Input Handling 이 새 Input System 전용이라 런타임에 터진다. `#if ENABLE_LEGACY_INPUT_MANAGER` 로 갈라 쓸 것
